### Needs to install pandoc and context (full live-tex distro)
### and then run make all

install:
  - sudo apt-get update -qq update
  - sudo install -y texlive
  - sudo install -y pandoc

script:
  make all

notifications:
  email: false
